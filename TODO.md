# TODO

- [ ] Update `TODO_EMAIL_TEMPLATES.md` with a “professional email testing” workflow (set `REMINDER_TO`, and curl examples for welcome/forgotPassword/reminder).
- [ ] (Optional) Cleanup unused `exampleRecipient` in `background-tasks/taskHandlers.js`.
- [ ] After edits: run background task service and verify via `GET /tasks` and reminder schedule logs.

