# TODO: /register-gateway UI theme consistency fix

- [ ] Refactor `login-signup/src/pages/GatewayRegistration.jsx` to use shared `AdminLayout` (standard admin sidebar/header)
- [ ] Replace page-specific blue sidebar/header markup with shared admin components
- [ ] Replace dark cards/panels with shared light card styles (rm-* / ui/Card)
- [ ] Replace Register Gateway + Add Badges/Devices buttons with shared `ui/Button` variants
- [ ] Restyle Setup Checklist card using shared light card styling (keep same 3-step checklist content/structure)
- [ ] Ensure all form logic/handlers/validation remain unchanged
- [ ] Quick run/build to ensure no compile errors

