# TODO_ADMIN_PANEL

- [ ] Update sidebar/navbar to include a link to a separate Admin Users page (e.g. /admin/users) visible only for Admin.
- [ ] Improve `AdminUsersPage.jsx` UI so it shows View / Edit / Delete actions (delete/edit currently placeholders).
- [ ] Add backend endpoints needed for Edit/Delete if missing (current backend only has GET /api/admin/users).
- [ ] Implement View/Edit/Delete actions on frontend using new backend endpoints.
- [ ] Add backend middleware to ensure only Admin can Edit/Delete.
- [ ] Test flow: login as seeded admin, open /admin/users, verify actions work.

